- name: Gather Machine Facts
  setup:
    gather_subset:
      - "all"
  delegate_to: "{{ ansible_host }}"
- debug:
    msg: "{{ ansible_facts }}"

- name: Update Packages
  ansible.builtin.yum:
    name: "*"
    state: latest
