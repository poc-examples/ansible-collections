- name: Gather Machine Facts
  setup:
  registry: ansible_facts
  delegate_to: "{{ ansible_host }}"
- debug:
    msg: "{{ ansible_facts }}"

- name: Update Packages
  ansible.builtin.yum:
    name: "*"
    state: latest
